{
  "scripts": {
    "prepare": "husky install",
    "lint:staged": "lint-staged",
    "lint": "cd client && npm run lint:staged"
  },
  "lint-staged": {
    "*": [
      "npm run lint",
      "npx prettier --write ."
    ]
  },
  "devDependencies": {
    "husky": "^8.0.3"
  },
  "dependencies": {
    "lint-staged": "^13.2.1"
  }
}
